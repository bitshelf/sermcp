# MCP 环境变量模板 — 复制为 .env 并按需填写（.env 已被 .gitignore 排除，绝不提交）
#
# 使用方法：
#   1. cp .env.example .env  （或直接 export 到 ~/.bashrc）
#   2. 填入真实值
#   3. 重启 Claude Code（或 /mcp 重连）
#
# .mcp.json 通过 ${VAR} 引用这里的变量。

# GitHub MCP Server 认证（github 服务器必需）
# 创建：https://github.com/settings/personal-access-tokens/new (fine-grained)
# 权限：Issues / Pull requests / Contents 至少 Read and write，Metadata Read-only（必选）
GITHUB_PERSONAL_ACCESS_TOKEN=github_pat_xxx
